{
  "name": "database",
  "version": "0.1.0",
  "private": true,
  "description": "Via Carraria Prisma schema, migrations, and Directus metadata",
  "license": "UNLICENSED",
  "type": "module",
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "seed": "prisma db seed",
    "directus:sync": "tsx prisma/directus-sync.ts",
    "studio": "prisma studio",
    "format": "prisma format",
    "validate": "prisma validate"
  },
  "dependencies": {
    "@prisma/client": "^6.15.0"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "prisma": "^6.15.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}